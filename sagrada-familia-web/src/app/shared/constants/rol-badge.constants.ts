import { BadgeColorKey } from './badge-color.constants';

export const ROL_BADGE_COLOR: Record<string, BadgeColorKey> = {
  Administrador: 'purple',
  Medico:        'primary',
  Padre:         'pink',
};
